export enum ActionTypes {
  INIT_CONVERATION = '[Conversation] Init conversation',
  INIT_CONVERATION_SUCCESS = '[Conversation] Init conversation success',
  INIT_CONVERATION_FAILURE = '[Conversation] Init conversation failure',

  LOAD_CONVERATIONS = '[Conversation] LOAD conversation',
  LOAD_CONVERATIONS_SUCCESS = '[Conversation] LOAD conversation success',
  LOAD_CONVERATIONS_FAILURE = '[Conversation] LOAD conversation failure',
}
